 <div class="col-md-6">
     <div class="mb-3">
         <label for="{{ $label }}" class="form-label">{{ __('message.' . $label) }}</label>
         <input type="{{ $type }}" class="form-control" name="{{ $label }}" id="{{ $label }}"
             aria-describedby="helpId" placeholder="{{ __('message.' . $label) }}">
     </div>
 </div>
